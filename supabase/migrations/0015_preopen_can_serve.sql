-- =====================================================================
-- プレオープン出勤シフト: 施術可否フラグ
-- =====================================================================
-- まだお客さんに施術できないスタッフ（例: 桑原）を、出勤表には残しつつ
-- 受付数（各枠の受付可能人数）から除外するためのフラグ。
-- can_serve = false … 出勤はするが施術に入らない（受付数に数えない）。
-- =====================================================================

alter table preopen_shifts
  add column if not exists can_serve boolean not null default true;
