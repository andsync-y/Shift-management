-- =====================================================================
-- スタッフ配布用の初期パスワード保存
-- =====================================================================
-- 管理者が発行した初期パスワードを、本人へ伝えるまで保持・表示するための列。
-- ログインID(メール)は auth.users 側を参照するためここには持たない。
-- ※ 平文保存のため、閲覧は super_admin のみ（profiles の RLS で制御済み）。
-- =====================================================================

alter table profiles add column if not exists initial_password text;
