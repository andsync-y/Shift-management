-- =====================================================================
-- 給与計算: 交通費（月額）
-- =====================================================================
-- 総支給（額面）に交通費を含めるため、スタッフごとの月額交通費を持たせる。
-- 0 または未設定なら 0 円として扱う。
-- =====================================================================

alter table profiles
  add column if not exists commute_allowance integer not null default 0;
