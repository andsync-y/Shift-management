# 給与計算（実打刻ベース）

オーナー管理画面でスタッフの月次給与を計算する。打刻（LINEの出勤〜退勤＝time_records）を
元データに、休憩・残業・深夜を反映して**総支給（額面）**を算出し、さらに
**控除（源泉所得税・雇用保険・社会保険）→ 差引支給額（手取り）**まで計算する。

## 計算ルール（`src/lib/payroll.ts` の `computePayroll`）

- **集計元**：`time_records`（clock_in〜clock_out, JST）。退勤未打刻は「打刻中」として給与に含めない。
- **時給は日付で決まる（期間別・全員一律）**：`WAGE_SCHEDULE`（`src/lib/payroll.ts`）。
  - 6/8〜6/19 → ¥1,060（講習）／ 6/20〜7/31 → ¥1,600（営業研修保証）／ **8/1〜 → ¥1,600（恒久フロア・減額なし）**。範囲外の日は各自の `hourly_wage`（fallback）。
  - 時給が日によって変わるため、賃金は**日ごとに計算して合算**（`wageForDate`）。
- **売上連動の時給テーブル**：`SALES_WAGE_TABLE` / `wageForSales(monthlySales)`（`src/lib/payroll.ts`）。
  - 個人の月間売上（指名抜き）に応じた時給：〜80万 ¥1,600（フロア）／〜90万 ¥1,800／〜100万 ¥2,000／〜110万 ¥2,200／〜120万 ¥2,400／〜130万 ¥2,600／〜140万 ¥2,800／140万超 ¥3,000。
  - ⚠️ 現状 `computePayroll` は `WAGE_SCHEDULE` の¥1,600フロアを適用（全員一律）。80万超の上位段は、**個人別の月間売上（FC KPI 担当別売上／サロンボード／Square）を給与計算に接続した段階**で `wageForSales` を用いて反映する（未接続）。
- **休憩自動控除**（その日の実働合計に対して）：8時間超→60分 / 6時間超→45分 を差し引く。
- **端数（1分単位）**：賃金は**1分単位（丸めなし）**で計算する（労基法の原則どおり）。
  初回給与（2026年6月分）から適用。
- **残業**：1日の実働（休憩控除後）が8時間を超えた分を 1.25倍（割増 +0.25）。
  - 判定は**1日8時間超のみ**（週40時間超の残業は未対応）。
- **深夜**：22:00〜翌5:00(JST) の労働に +0.25 加算（`nightMinutes` で区間重なりを算出）。
  - 深夜かつ残業が重なる時間は 基本1.0＋残業0.25＋深夜0.25＝**1.5倍**相当。
- **指名バック**：`指名バック = 指名本数 × 3,000円（全員固定・指名料の税抜相当＝税引後100%還元）`。
  - 単価は `NOMINATION_BACK_RATE`（`src/lib/payroll.ts`・既定3000）。スタッフ個別設定は廃止
    （旧 `profiles.nomination_back_rate` 列は残置・未使用）。
  - 指名本数は**給与画面の表で月ごとに手入力**（`nomination_counts`・フォーカスアウトで自動保存）。
  - **「FC指名数を取込んで給与確定」ボタン**で、本部KPIスナップショットの担当別 指名数を対象月の指名本数に
    一括取込できる（`applyFcNominations`・表示名で突合）。**対象月のKPIを持つスナップショットを検索**する
    ため、過去月も月内に取得したスナップショットが残っていれば取込可。見つからない場合は
    当月＝「日次取得後に再実行」／過去月＝「遡取得は不可・手入力で対応（入力済みなら取込不要）」を案内する。
- **回数券バック**（**2026年7月分から運用**）：`回数券バック = 本数連動の段階単価 × 本数`（`kaisukenBack` / `KAISUKEN_BACK_TIERS`・`src/lib/payroll.ts`）。
  - 段階単価（当月の回数券本数で決まる）：**1〜3本 ¥1,000 ／ 4〜7本 ¥2,000 ／ 8本〜 ¥3,000**。1本目から支給・0本なら0。
  - 判定は「販売率(%)」ではなく「**本数（絶対数）**」＝多く売るほど単価UP。新規を多く取る動機と同方向で、%の小サンプル・新規敬遠を回避。
  - **本数 = 新規販売＋更新販売の合計**。給与画面の表で月ごとに手入力（`kaisuken_counts`・フォーカスアウトで自動保存）。
  - 時給とは別の「上乗せ」（基本給からの減額はしない）。指名バックとも別立て。
- **給与明細の印刷**：「🖨 給与明細を印刷」→ `/admin/payroll/print?month=`（**A4横**・余白16×20mm・1人1枚・`window.print()`）。
  - 形式は一般的な**給与明細書グリッド**（勤務＝日数/実働/残業/深夜 → 支給＝基本給/残業・深夜手当/
    通勤費/バック/支給額合計 → 控除＝健保/厚年/雇用保険/社会保険計/課税対象額/所得税/住民税/控除計 →
    **差引支給額**）。見出しに氏名・支給日（翌月15日）・店舗名を表示。
- **給与明細のPDF・LINE送信**（`src/lib/payslip/`・`/api/payroll/payslips`）：
  - 「控除・差引支給」表の各行の**「PDF」リンク**で1人分をダウンロード、見出しの
    **「全員分PDF」**で全員を1ファイル（1人1ページ）でダウンロード（GET・オーナーのみ）。
  - **「📨 明細PDFをLINE送信」ボタン**で、全員分のPDFを生成→非公開バケット `payslips` に保存
    （`payslips/YYYY-MM/<staffId>.pdf`・上書き）→**署名付きURL（60日有効）を各自のLINEへ送信**（POST）。
    LINE未連携のスタッフは結果メッセージに表示される。
  - 様式は印刷ページと同じ給与明細書グリッド（A4横・pdf-lib）。日本語フォントは
    `public/fonts/NotoSansJP-Medium-sub.ttf`（Noto Sans JP のサブセット・SIL OFL）を丸ごと埋め込む
    （pdf-lib のサブセット機能はこのフォントでグリフ欠落を起こすため使用しない）。
  - 要マイグレーション：`0039_payslips_bucket.sql`（payslips バケット作成）。
- **総支給** = 基本(全労働×時給) + 残業割増 + 深夜割増 + **交通費** + **指名バック** + **回数券バック**。
  - 控除（源泉・雇用保険・社保）→ 手取りは下記「控除・差引支給」を参照。
- **交通費（距離ベース・推奨）**：スタッフに **片道距離(km)** を設定すると、
  `交通費 = 片道距離 × 2(往復) × 15円/km × 当月の勤務日数(実打刻で出退勤がそろった日)` で自動計算。
  単価は `COMMUTE_RATE_PER_KM`（`src/lib/payroll.ts`・既定15）。片道距離が空のときだけ
  従来の月額固定 `commute_allowance`（定期券など）を使う。勤務日数は `computePayroll` の `workedDays`。
- 賃金は分単位で集計し、最後に円へ四捨五入。
- **週平均（実績）**：月内の各週（月〜日）の実働合計を、勤務のあった週数で平均（`avgWeeklyMin`）。
  一覧の `週平均` 列に表示。

## 控除・差引支給（手取り）（`src/lib/deductions.ts` の `computeDeductions`）

給与画面の「控除・差引支給（手取り）」セクションで、総支給から控除を差し引いた
**差引支給額（手取り）** を計算する。

- **計算パイプライン**：総支給 → 雇用保険 → 社会保険 → 課税対象（非課税交通費・社保控除後）→ 源泉所得税 → **差引支給**。
- **雇用保険**：`総支給（交通費込み）× 0.5%`（令和8年度・一般の事業・被保険者負担。
  `EMP_INSURANCE_RATE`・年度改定に注意）。端数は50銭以下切捨・50銭超切上。
  スタッフ設定の「雇用保険 加入」がオフなら0。
- **社会保険（健保・厚年）**：スタッフ設定で「社保加入」をオンにした人のみ。
  当月報酬（交通費込み）から**標準報酬月額**を求め（`standardMonthlyRemuneration`・健保50等級表）、
  健保 `KENPO_RATE`（協会けんぽ岐阜・**当年度告示に要更新**）／介護 `KAIGO_RATE`（40〜64歳・
  「介護保険 第2号」オン時のみ）／厚年 18.3% を**労使折半**して本人負担分を控除。
  - ⚠️ 本来の標準報酬月額は資格取得時決定・定時決定（4〜6月平均）で固定される。
    当月報酬から求める本実装は**簡易方式（目安）**。正式運用は社労士に確認。
- **源泉所得税（令和8年分・全自動）**：スタッフ設定の**税区分（甲欄/乙欄）**と**扶養親族等の数**で、
  国税庁「給与所得の源泉徴収税額表（令和8年分）月額表」により自動計算（`src/lib/tax-table-r8.ts`・
  公式PDFから生成した全231行＋高額域の計算式を収録）。
  - **甲欄** ＝ 扶養控除等申告書を当店に提出済み（提出先は1社のみ）。課税対象 < ¥105,000 → ¥0。
    扶養親族等の数（0〜7人の列・7人超は1人につき▲¥1,610）に対応。
  - **乙欄** ＝ 未提出（他社が本業のダブルワーク等・既定値）。課税対象 < ¥105,000 → 3.063%（1円未満切捨）、
    以上は税額表の乙欄。
  - ¥740,000以上は表の備考どおり「アンカー税額＋超過分×率」で計算。
  - **手入力（上書き）**：個別事情があるときは給与画面の源泉欄に入力（`income_tax_overrides`・
    入力があれば常に自動より優先・空欄に戻すと自動に復帰。上書き中は「上書」バッジ表示）。
  - ⚠️ 税額表は毎年改定される。**令和9年分が公表されたら `tax-table-r8.ts` を更新**すること。
- **非課税交通費**：距離ベースの交通費は全額非課税として課税対象から除外
  （マイカー通勤の非課税限度の細目は未実装・運用で確認）。
- **給与明細（印刷）**にも控除内訳と差引支給額を表示。源泉未確定の人は「暫定」と注記。
- **給与振込データ（全銀）**の振込額は**差引支給（手取り）**。源泉「要入力」が残っている
  スタッフは源泉¥0の暫定額になるため、画面の警告を確認してから出力する。
- **スタッフ設定**（「スタッフ管理」→ 編集 → 税・社会保険）：税区分（甲/乙）・扶養親族等の数・
  雇用保険加入・社保加入・介護保険第2号。

### 社会保険 加入判定（目安）

専用セクション「社会保険 加入判定」を給与画面に表示する（`shahoJudge`）。

- **週所定**：契約上の週の所定労働時間 `profiles.contracted_weekly_hours`（「スタッフ管理」で設定）。
  未設定なら**実績の週平均で代用**（`代用` タグ）。
- **判定**：**週30時間以上で `加入対象`**、未満は `対象外`（`shahoJudge`）。
  - 週時間は「週所定（あれば）／無ければ実績週平均」を使う。
- 本判定は**目安**。最終判断は社労士等に確認すること。

> 労基法は1分単位での支払いが原則であり、本システムはこの原則どおり1分単位で計算する
> （丸めに関する法的リスクは無い）。

## 画面・導線

- 管理ナビ「給与計算」→ `/admin/payroll`。月セレクタで対象月を切替。
- スタッフ別の集計表（実働/残業/深夜/基本/各割増/交通費/総支給）と、
  名前をタップで開く**日別明細**（出退勤・実働・休憩・残業・深夜）。
- **時給・交通費は「スタッフ管理」→ スタッフ編集**で設定（`hourly_wage` / 片道距離 `commute_distance_km` / 月額 `commute_allowance`）。

## DBマイグレーション（要適用）

- `supabase/migrations/0017_commute_allowance.sql`：`profiles.commute_allowance`（月額交通費）。
- `supabase/migrations/0034_commute_distance.sql`：`profiles.commute_distance_km`（片道距離・距離ベース交通費）。
- `supabase/migrations/0027_contracted_hours.sql`：`profiles.contracted_weekly_hours`（週の所定労働時間・社保判定用）。
- `supabase/migrations/0029_nomination_back.sql`：`profiles.nomination_back_rate`（指名バック単価）＋ `nomination_counts`（月別指名本数）。
- `supabase/migrations/0037_kaisuken_back.sql`：`kaisuken_counts`（月別 回数券販売本数）。回数券バック（本数連動）の元データ。
- `supabase/migrations/0038_payroll_deductions.sql`：`profiles` に税・保険設定（`tax_column` / `dependents_count` / `emp_insurance_enrolled` / `shaho_enrolled` / `kaigo_applicable`）＋ `income_tax_overrides`（月別 源泉の手入力）。
- `supabase/migrations/0031_bank_account.sql`：`profiles` に振込先口座（銀行/支店コード・預金種目・口座番号・受取人カナ）。
- `supabase/migrations/0039_payslips_bucket.sql`：給与明細PDFの非公開バケット `payslips`。

## 給与振込データ（全銀フォーマット・SMBC総合振込）

- 給与画面「給与振込データ（全銀フォーマット）」で、当月の給与から**振込ファイル**を生成
  （`src/lib/zengin.ts`・固定長120バイト/レコード・Shift_JIS）。ダウンロードして三井住友の
  ビジネスバンキング（Web21／ValueDoor）にアップロード→実行する（API `/api/payroll/transfer`）。
- **形式の切替**：総合振込（種別コード21・既定）／給与振込（種別コード11・`?kind=salary`）。
  **Web21のアップロード先メニューと形式を一致させる**こと（総合振込メニュー⇔種別21、
  給与／賞与振込メニュー⇔種別11。不一致だと「種別コードが不正」）。給与振込は手数料が安い
  一方、データの締めが早い（通常3営業日前）。
- 振込金額＝**差引支給（手取り・控除後）**。口座情報が未登録のスタッフは対象外（画面に警告表示）。
- 各スタッフの口座は「スタッフ管理」で登録（受取人カナは半角に自動変換）。
- **依頼人名（`ZENGIN_CONSIGNOR_NAME`）は出金口座の名義と完全一致させる**（スペースの有無も
  一致判定される。例: `ｶ) ｱﾝﾄﾞｼﾝｸ`＝「ｶ)」の後に半角スペース）。不一致だと銀行画面で警告が出る。
- 委託者情報は環境変数：`ZENGIN_CONSIGNOR_CODE`（委託者コード）/ `ZENGIN_CONSIGNOR_NAME`（依頼人カナ）/
  `ZENGIN_BRANCH_CODE`（自社支店）/ **`ZENGIN_ACCOUNT_NUMBER`（引落口座番号7桁・Web21必須）** /
  `ZENGIN_ACCOUNT_TYPE`（預金種目・既定1=普通）/ `ZENGIN_BANK_CODE`（既定0009）/ `ZENGIN_BANK_NAME` / `ZENGIN_BRANCH_NAME`。
  - ヘッダーの96〜103桁目に依頼人口座（種目＋口座番号）が入る。未設定だと Web21 で
    「フォーマットエラー（1行目、口座番号）」になる。
- 拘束/実働：給与一覧に「拘束」（出勤〜退勤合計・勤怠管理と一致）と「実働」（休憩控除後・1分単位）を併記。
- ⚠️ 銀行が受け付けるか必ず**少人数で試験送金**して確認すること（フォーマット細部は銀行仕様に依存）。

## 勤怠管理（給与の元データ）

`/admin/timecards`（オーナー専用）。打刻（`time_records`）の確認・修正・追加。

- **出退勤は「日付＋時刻」で編集する**（`datetime-local`）。
  退勤の打刻を忘れて後日入力する場合や、日跨ぎの深夜勤務（例 22:30→翌1:00）に対応するため、
  時刻だけでなく**日付も変更できる**。
- **入力ミスの検知**：
  - 勤務が **24時間を超える**とサーバー側で保存を拒否し、「退勤の日付をご確認ください」と返す
    （`MAX_SHIFT_HOURS`・`validateRange`）。退勤漏れを翌日以降に入力した際の異常値を防ぐ。
  - 画面でも保存前に、24時間超は**赤枠＋「◯h」バッジ**、日跨ぎは**「翌日」バッジ**で気づけるようにしている。
- 「退勤漏れのみ」チェックで未退勤（打刻中）の記録だけを絞り込める。
- スタッフ別集計は**拘束時間ベース**（休憩控除なしの概算）。賃金の元になる実働は給与画面を見る。

## 計算の集約（重要）

給与の計算は **`src/lib/compute-staff-payroll.ts` の `computeStaffPayroll()` に集約**している。
以前は「打刻→給与→バック加算→控除」の同じ組み合わせが4か所にコピーされており、
仕様変更のたびに4か所を直す必要があった（直し忘れると画面・明細・振込額がずれる）。

呼び出し側（この4つ以外から給与計算をしないこと）：

| 呼び出し元 | 用途 |
|---|---|
| `src/app/admin/payroll/page.tsx` | 給与画面の一覧・控除表 |
| `src/app/admin/payroll/print/page.tsx` | 給与明細の印刷 |
| `src/lib/payslip/data.ts` | 給与明細PDF・LINE送信 |
| `src/app/api/payroll/transfer/route.ts` | 全銀振込データ（振込額＝差引支給） |

**計算仕様を変えるときは `computeStaffPayroll()` だけを直す。** 呼び出し側は結果を表示するだけ。

## テスト

`npm test`（Node標準のテストランナー。追加の依存なし）。

- `tests/payroll.test.ts` — `computePayroll` の時間集計・休憩・残業・深夜・期間別時給・交通費
- `tests/deductions.test.ts` — 雇用保険・社保・源泉（令和8年分税額表）・手取り
- `tests/compute-staff-payroll.test.ts` — 集約関数（総支給の組み立て〜控除まで）

⚠️ **給与ロジックを変更する前に必ず `npm test` が通ることを確認し、変更後も通ることを確認する。**
テストには2026年7月の実績（KIYO・DAYAN・AINAの明細PDFの金額）を1円単位で固定してあり、
**過去の給与が変わっていないこと（後方互換）**の担保になっている。

## 関連ファイル

| ファイル | 役割 |
|---|---|
| `src/lib/payroll.ts` | 給与計算ロジック（休憩/残業/深夜/総支給） |
| `src/lib/deductions.ts` | 控除エンジン（雇用保険/社保/源泉→手取り） |
| `src/lib/tax-table-r8.ts` | 源泉徴収税額表（令和8年分）月額表（公式PDFから生成） |
| `src/app/admin/payroll/page.tsx` | 給与計算画面（集計表＋控除・手取り＋日別明細） |
| `supabase/migrations/0017_commute_allowance.sql` | 交通費（月額）列 |
| `src/app/admin/staff/[id]/ProfileEditor.tsx` | 時給・交通費の設定 |

## 今後の拡張候補

- 標準報酬月額の正式運用（資格取得時決定・定時決定の固定値を持つ）
- 年末調整・源泉徴収票
- 休日割増（法定休日）/ 週40時間超の残業
- 給与明細のPDF/CSV出力
- 確定シフト基準との切替
